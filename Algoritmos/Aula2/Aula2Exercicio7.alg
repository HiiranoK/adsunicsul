algoritmo CambioDolar
    real valorEmBRL, valorEmUSD, cotaçãoAtual;
    início
        escreva("A cotação atual está: "+ cotaçãoAtual +" quantos dollares pretende converter?");
        leia(valoremUSD);
        valorEmUSD * cotaçãoAtual = valorEmBRL;
        escreva("A conversão foi realizada com sucesso, seus" + valorEmUSD + " foram trocados por " + valorEmBRL +" reais");
    fim