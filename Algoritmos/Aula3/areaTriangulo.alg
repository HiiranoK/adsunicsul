algoritmo Triangulo
real base, altura
inicio
    escreva("informe o valor da base");
    leia(base);
    escreva("informe o valor da altura");
    leia(altura)
    calculaArea(base,altura)
fim

real calculaArea(real base, real altura){inicio
    retorne (base*altura/2)
}fim